# DESIGN.md — 项目设计文档

> 本文件是高层设计文档的起点，供团队与 AI 模型共享系统架构上下文。  
> 在 prompt 中引用本文件时，只需粘贴相关章节，控制 token 在 2–4 KB 以内。  
> 重大设计变更需同步更新本文件并在 [DECISIONS.md](./DECISIONS.md) 中记录决策。

---

## 项目概述

| 字段 | 内容 |
|------|------|
| **项目名称** | \<项目名\> |
| **版本** | v\<X.Y.Z\> |
| **最后更新** | \<YYYY-MM-DD\> |
| **技术栈** | \<语言、框架、数据库、基础设施\> |
| **团队规模** | \<X 名后端 + Y 名前端 + Z 名 SRE\> |
| **SLA** | \<如 99.9% uptime，P99 < 200ms\> |

---

## 系统架构（高层）

```
┌─────────────────────────────────────────────────────────┐
│                    <项目名> 系统架构                       │
├───────────┬───────────────────────┬─────────────────────┤
│  前端层    │       后端层           │      数据层          │
│  <组件>   │  <服务/API>            │  <数据库/缓存>       │
└───────────┴───────────────────────┴─────────────────────┘
```

> 请替换上方示意图为实际架构图（ASCII art 或链接到图表文件）。

---

## 核心模块

| 模块 | 路径 | 职责 | 主要依赖 |
|------|------|------|----------|
| \<模块1\> | `src/<module1>` | \<职责\> | \<依赖\> |
| \<模块2\> | `src/<module2>` | \<职责\> | \<依赖\> |

---

## 关键接口（API Contract）

### \<接口名称\>

```
方法：GET / POST / PUT / DELETE
路径：/api/v1/<resource>
认证：Bearer Token / OAuth2.0 / API Key
```

**请求**：
```json
{
  "field1": "string",
  "field2": 0
}
```

**响应**：
```json
{
  "status": "success",
  "data": {}
}
```

---

## 数据库 Schema（核心表）

```sql
-- <表名>
CREATE TABLE <table_name> (
  id         BIGSERIAL PRIMARY KEY,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  -- <其他字段>
);
```

---

## 非功能需求

| NFR | 目标值 | 当前值 | 状态 |
|-----|--------|--------|------|
| 可用性 | 99.9% | \<X%\> | 🟢/🟡/🔴 |
| P99 延迟 | < 200ms | \<Xms\> | 🟢/🟡/🔴 |
| 并发用户 | \<N\> | \<N\> | 🟢/🟡/🔴 |
| 测试覆盖率 | > 80% | \<X%\> | 🟢/🟡/🔴 |

---

## 安全考量

- **认证/授权**：\<方案描述\>
- **数据加密**：\<传输加密 + 存储加密方案\>
- **敏感数据处理**：\<脱敏/审计策略\>
- **已知安全风险**：\<列出已知风险及缓解措施\>

---

## 部署架构

| 环境 | 基础设施 | 部署方式 | 监控 |
|------|----------|----------|------|
| Dev | \<本地/云\> | \<Docker Compose / k8s\> | \<工具\> |
| Staging | \<云服务\> | \<CI/CD Pipeline\> | \<工具\> |
| Production | \<云服务\> | \<蓝绿/Canary\> | \<工具\> |

---

## 变更历史

| 版本 | 日期 | 变更摘要 | 变更人 | 相关决策 |
|------|------|----------|--------|----------|
| v0.1 | \<YYYY-MM-DD\> | 初始化设计文档 | \<姓名\> | — |

---

## 待讨论事项（Open Questions）

- [ ] \<待确认问题1\>（预计解决日期：\<YYYY-MM-DD\>）
- [ ] \<待确认问题2\>

---

> **提示**：在 AI prompt 中引用本文件时，建议只粘贴与当前任务相关的章节，  
> 并在开头注明 `参考 DESIGN.md vX.Y — <章节名>` 以便后续追溯。
